const a = process.argv.slice(2)[0]
console.log(a)


// Also there's a package named minimist, it can be used to detect exact argument given
// for example name=moatassem then we can get moatassem by using .name along with some other syntax

