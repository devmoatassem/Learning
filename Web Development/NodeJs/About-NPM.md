## Some necessary npm CLI commands

```
npm init  
npm install (-g for global installation) (for one package give pkg name) (--save-dev to add to dev dependencies) (--save-optional to add library to optional dependecies)

```
